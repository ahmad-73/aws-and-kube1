# rdicidr-service.yml
apiVersion: v1
kind: Service
metadata:
  name: rdicidr-service
  namespace: production
spec:
  clusterIP: None  # for StatefulSet headless service
  selector:
    app: rdicidr
  ports:
    - port: 80
      targetPort: 80
